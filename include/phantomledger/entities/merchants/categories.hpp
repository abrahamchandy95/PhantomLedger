#pragma once

#include <string>
#include <vector>

namespace PhantomLedger::entities::merchants {

struct Categories {
  std::vector<std::string> names;
};

} // namespace PhantomLedger::entities::merchants
